name: "Setup env"
description: "Install required environment components"
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v5
    - name: Set up JDK 11
      uses: actions/setup-java@v5
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven
